on: 
  push:
    branches: [ "main", "master" ]
  workflow_dispatch: 

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Windows vcpkg and Build
      run: |
        git clone https://github.com/microsoft/vcpkg.git
        .\vcpkg\bootstrap-vcpkg.bat
        .\vcpkg\vcpkg install libheif:x64-windows
        cmake -B build_win -S . -DCMAKE_TOOLCHAIN_FILE=.\vcpkg\scripts\buildsystems\vcpkg.cmake -A x64
        cmake --build build_win --config Release

    - name: Upload the finished DLL
      uses: actions/upload-artifact@v4
      with:
        name: HeicDecoder-Plugin
        path: build_win/Release/HeicDecoder.dll